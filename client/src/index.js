//The entry point for the React app that renders the App.js component into the root DOM element.
//Starting pont for the React application, where the main component is rendered into the HTML document.